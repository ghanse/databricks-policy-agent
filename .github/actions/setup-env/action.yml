name: "Setup Environment"
description: "Install a pinned uv that resolves against the default PyPI index."
runs:
  using: "composite"
  steps:
    - name: Setup uv
      uses: astral-sh/setup-uv@5a095e7a2014a4212f075830d4f7277575a9d098 # v7.3.1
      with:
        version: "0.11.2"
